ctags for ObjectiveC
====================
A tag file generator for ObjectiveC written in Python. Currently, it only supports generate tags for object and class method. The output format works well with ctrlp vim plugin.

